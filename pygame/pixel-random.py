#! /usr/bin/env python3

# Plot random pixels on the screen.
# Twinkle, Twinkle… Little… :)

import pygame, random
from pygame.locals import *

WHITE = (255,255,255)

WIDTH = 600
HEIGHT = 400

def main():
    while True:

# Use the Modules for game ability and randomness
# Define Window Dimentions (in a Var Ideally)
# Set the display mode and initialize the screen object
# Endless Loop until we click on the Quit button of the Window (the red x)
# The starfield should be filled with Random Integers (careful of the X/Y size)
# Ok I will be nice here is actual REAL code :P
# Draw the pixel at the random co-ordinate (use the method: __init__.set_at())
# Flip it:
    pygame.display.flip()
